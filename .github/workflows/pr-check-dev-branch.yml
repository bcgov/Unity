name: Dev - CI & Unit Tests

permissions:
  contents: read
  pull-requests: write

on:
  pull_request:
    branches:
      - dev

jobs:
  check-dev-branch:
    runs-on: ubuntu-latest
    outputs:
      branch-allowed: ${{ steps.branch-check.outputs.allowed }}
    steps:
      - name: Validate PR source branch
        id: branch-check
        run: |
          echo "Source branch: ${GITHUB_HEAD_REF}"
          if [[ ${GITHUB_HEAD_REF} =~ ^feature/ ]] || \
             [[ ${GITHUB_HEAD_REF} =~ ^hotfix/ ]]  || \
             [[ ${GITHUB_HEAD_REF} =~ ^bugfix/ ]]  || \
             [[ ${GITHUB_HEAD_REF} == "test" ]]     || \
             [[ ${GITHUB_HEAD_REF} == "main" ]]; then
              echo "allowed=true" >> $GITHUB_OUTPUT
          else
              echo "❌ ERROR: PR into dev must come from:"
              echo "feature/*, hotfix/*, bugfix/*, test, or main"
              echo "allowed=false" >> $GITHUB_OUTPUT
              exit 1
          fi

  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: check-dev-branch
    if: needs.check-dev-branch.outputs.branch-allowed == 'true'

    steps:

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install .NET 9 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "9.0.x"

      - name: Restore NuGet packages
        run: dotnet restore applications/Unity.GrantManager/Unity.GrantManager.sln

      - name: Run unit tests with coverage (multi-project safe)
        id: run-tests
        continue-on-error: true
        run: |
          mkdir -p applications/Unity.GrantManager/TestResults

          dotnet test applications/Unity.GrantManager/Unity.GrantManager.sln \
          --logger "trx;LogFileName=unity-tests-${{ github.run_id }}.trx" \
          --results-directory applications/Unity.GrantManager/TestResults \
          /p:CollectCoverage=true \
          /p:CoverletOutput=../TestResults/ \
          /p:MergeWith=../TestResults/coverage.json \
          /p:CoverletOutputFormat="cobertura,json"

      - name: Debug TestResults directory
        run: |
          echo "📁 Listing TestResults:"
          ls -l applications/Unity.GrantManager/TestResults/
          echo "Checking first coverage file:"
          head -40 $(ls applications/Unity.GrantManager/TestResults/coverage-*.xml | head -1) || echo "Coverage file missing"

      - name: Install XML parser for TRX extraction
        run: sudo apt-get install -y xmlstarlet

      - name: Extract test result counts from TRX
        id: extract-results
        run: |
          TRX_FILE=$(find applications/Unity.GrantManager/TestResults -name "*.trx" | head -1)
          echo "Using TRX: $TRX_FILE"
          
          if [ -z "$TRX_FILE" ] || [ ! -f "$TRX_FILE" ]; then
            echo "❌ ERROR: TRX file not found in TestResults directory"
            ls -la applications/Unity.GrantManager/TestResults/
            exit 1
          fi

          PASSED=$(xmlstarlet sel -t -v "count(//UnitTestResult[@outcome='Passed'])" "$TRX_FILE")
          FAILED=$(xmlstarlet sel -t -v "count(//UnitTestResult[@outcome='Failed'])" "$TRX_FILE")
          SKIPPED=$(xmlstarlet sel -t -v "count(//UnitTestResult[@outcome='NotExecuted'])" "$TRX_FILE")

          echo "passed=$PASSED"  >> $GITHUB_OUTPUT
          echo "failed=$FAILED"  >> $GITHUB_OUTPUT
          echo "skipped=$SKIPPED" >> $GITHUB_OUTPUT

      - name: Generate detailed test summary markdown
        id: test-summary
        run: |
          TRX_FILE=$(find applications/Unity.GrantManager/TestResults -name "*.trx" | head -1)
          
          if [ -z "$TRX_FILE" ] || [ ! -f "$TRX_FILE" ]; then
            echo "❌ ERROR: TRX file not found for test summary"
            exit 1
          fi

          echo "### Detailed Test Results" > summary.md
          echo "| Test Name | Outcome |" >> summary.md
          echo "| --------- | ------- |" >> summary.md

          xmlstarlet sel -t -m "//UnitTestResult" \
            -v "concat(@testName, '|', @outcome)" -n "$TRX_FILE" |
            while IFS="|" read -r name outcome; do
              echo "| $name | $outcome |" >> summary.md
            done

          echo "summary<<EOF" >> $GITHUB_OUTPUT
          cat summary.md >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Install ReportGenerator
        run: dotnet tool install -g dotnet-reportgenerator-globaltool

      - name: Add dotnet tools to PATH
        run: echo "$HOME/.dotnet/tools" >> $GITHUB_PATH

      - name: Convert coverage to HTML (safe with multiple files)
        run: |
          mkdir -p applications/Unity.GrantManager/TestResultsHtml
          reportgenerator \
            -reports:"applications/Unity.GrantManager/TestResults/coverage-*.xml" \
            -targetdir:"applications/Unity.GrantManager/TestResultsHtml" \
            -reporttypes:Html

      - name: Upload HTML test report
        uses: actions/upload-artifact@v4
        with:
          name: unity-tests-html
          path: applications/Unity.GrantManager/TestResultsHtml/

      - name: Upload detailed test summary markdown
        uses: actions/upload-artifact@v4
        with:
          name: unity-tests-summary-md
          path: summary.md

      - name: Determine pass/fail outcome
        id: final-result
        run: |
          if [[ ${{ steps.extract-results.outputs.failed }} -gt 0 ]]; then
            echo "passed=false" >> $GITHUB_OUTPUT
          else
            echo "passed=true" >> $GITHUB_OUTPUT
          fi

      - name: Generate badge
        id: badge
        run: |
          if [[ "${{ steps.final-result.outputs.passed }}" == "true" ]]; then
            echo "badge=![Tests](https://img.shields.io/badge/Tests-Passing-brightgreen)" >> $GITHUB_OUTPUT
          else
            echo "badge=![Tests](https://img.shields.io/badge/Tests-Failing-red)" >> $GITHUB_OUTPUT
          fi

      - name: Add PR comment with full test summary
        uses: peter-evans/create-or-update-comment@v4
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ## 🧪 Unit Test Results
            ${{ steps.badge.outputs.badge }}

            ### 📊 Summary
            | Result | Count |
            |--------|-------|
            | ✅ Passed | `${{ steps.extract-results.outputs.passed }}` |
            | ❌ Failed | `${{ steps.extract-results.outputs.failed }}` |
            | ⚠️ Skipped | `${{ steps.extract-results.outputs.skipped }}` |

            ${{ steps.test-summary.outputs.summary }}

            ### 📄 HTML Report
            Download the full coverage report from workflow artifacts.

      - name: Send Microsoft Teams Notification
        run: |
          PASSED=${{ steps.final-result.outputs.passed }}
          TOTAL_PASSED=${{ steps.extract-results.outputs.passed }}
          TOTAL_FAILED=${{ steps.extract-results.outputs.failed }}
          TOTAL_SKIPPED=${{ steps.extract-results.outputs.skipped }}

          PR_TITLE="${{ github.event.pull_request.title }}"
          PR_URL="${{ github.event.pull_request.html_url }}"
          PR_AUTHOR="${{ github.event.pull_request.user.login }}"
          SOURCE_BRANCH="${{ github.head_ref }}"
          TARGET_BRANCH="${{ github.base_ref }}"

          if [ "$PASSED" = "true" ]; then
            STATUS="🟢 Tests Passed - PR: $PR_TITLE (by $PR_AUTHOR) | $SOURCE_BRANCH → $TARGET_BRANCH"
            COLOR="00FF00"
          else
            STATUS="🔴 Tests Failed - PR: $PR_TITLE (by $PR_AUTHOR) | $SOURCE_BRANCH → $TARGET_BRANCH"
            COLOR="FF0000"
          fi

          JSON=$(cat <<EOF
          {
            "@type": "MessageCard",
            "@context": "http://schema.org/extensions",
            "themeColor": "$COLOR",
            "summary": "Unit Test Results",
            "title": "$STATUS",
            "sections": [
              {
                "facts": [
                  { "name": "Passed", "value": "$TOTAL_PASSED" },
                  { "name": "Failed", "value": "$TOTAL_FAILED" },
                  { "name": "Skipped", "value": "$TOTAL_SKIPPED" }
                ],
                "markdown": true
              }
            ]
          }
          EOF
          )

          curl -H "Content-Type: application/json" -d "$JSON" "YOUR_TEAMS_WEBHOOK_URL_HERE"

      - name: Fail if tests failed
        if: steps.final-result.outputs.passed == 'false'
        run: |
          echo "❌ Unit tests failed."
          exit 1
